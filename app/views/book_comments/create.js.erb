$("#comments_area").html("<%= j(render partial: 'book_comments/index', locals: {books: @book, book_comment: @book_comment}) %>");
$("textarea").val('')

// #comments_areaの後に_<%= @comment.id %>のようなどのidを特定する記述をするのは好ましくない